"""Baseline algorithms used in the comparison tables.

Each baseline is intentionally a small self-contained module so the paper
can cite them cleanly. They share the simulator's typing conventions with
``research.simulator`` but never touch production state.
"""
